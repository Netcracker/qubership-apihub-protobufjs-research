syntax = "proto3";
 
package user;
 
message UserProfile {
    string user_id = 1;
    string name = 2;
 
    // Marking the email field as deprecated
    string email = 3 [deprecated = true];
 
    // A new preferred field
    string contact_email = 4;
}